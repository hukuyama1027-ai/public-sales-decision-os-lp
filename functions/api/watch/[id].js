import { requireClientKey } from '../../../lib/client-token.js';
import { recordEvent } from '../../../lib/events.js';
import { cleanText, json } from '../../../lib/validation.js';
export async function onRequestDelete({request,env,params}){const k=await requireClientKey(request);if(!k)return json({ok:false,error:'TOKEN_REQUIRED'},400);const id=cleanText(params.id,128);const existing=await env.DB.prepare('SELECT 1 AS ok FROM watch_items WHERE client_key=? AND opportunity_id=?').bind(k,id).first();await env.DB.prepare('DELETE FROM watch_items WHERE client_key=? AND opportunity_id=?').bind(k,id).run();if(existing){try{await recordEvent(env.DB,{event_type:'watch_remove',session_id:cleanText(request.headers.get('x-session-id'),80)||'anonymous',client_key:k,entity_type:'opportunity',entity_id:id});}catch{}}return json({ok:true,watched:false});}
