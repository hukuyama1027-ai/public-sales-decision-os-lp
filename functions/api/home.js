import { requireClientKey } from '../../lib/client-token.js';
import { publicOpportunity } from '../../lib/db.js';
import { matchOpportunity } from '../../lib/matcher.js';
import { nextActions } from '../../lib/next-actions.js';
import { json } from '../../lib/validation.js';
export async function onRequestGet({request,env}){if(!env.DB)return json({ok:false,error:'DB_UNAVAILABLE'},503);const k=await requireClientKey(request);const recent=await env.DB.prepare('SELECT * FROM opportunities ORDER BY COALESCE(announced_at,\'\') DESC,fetched_at DESC LIMIT 30').all();const newItems=(recent.results||[]).slice(0,5).map(publicOpportunity);let profile=null,recommendations=[],watchItems=[],todayActions=[];if(k){profile=await env.DB.prepare('SELECT * FROM company_profiles WHERE client_key=?').bind(k).first();const w=await env.DB.prepare('SELECT o.*,w.saved_at FROM watch_items w JOIN opportunities o ON o.id=w.opportunity_id WHERE w.client_key=? ORDER BY w.saved_at DESC LIMIT 5').bind(k).all();watchItems=(w.results||[]).map(publicOpportunity);if(profile){recommendations=(recent.results||[]).map(o=>({source:publicOpportunity(o),decision:matchOpportunity(profile,o)})).sort((a,b)=>b.decision.score-a.decision.score).slice(0,5);for(const x of watchItems.slice(0,3))todayActions.push({opportunity_id:x.id,title:x.title,action:nextActions(x,profile)[0]});if(!todayActions.length&&recommendations[0])todayActions.push({opportunity_id:recommendations[0].source.id,title:recommendations[0].source.title,action:'おすすめ案件の原典を確認する'});}}
const d=await env.DB.prepare("SELECT * FROM opportunities WHERE deadline_at IS NOT NULL AND deadline_at>=date('now') ORDER BY deadline_at LIMIT 5").all();return json({ok:true,profile_status:profile?'complete':'missing',recommendations,new_items:newItems,deadline_items:(d.results||[]).map(publicOpportunity),watch_items:watchItems,today_actions:todayActions});}
